1. site.json in site container affect the outputs in webpage


copy site-template to your site name (SPRUCE)
set the sites.json in runner/config/sites.json
set docker-compose.yml

set new site
    1. 